# Compose carica entrambi i file: lo stack base (con include del kickstart) +
# l'override che inietta le dashboard IoT nella Grafana del kickstart.
# Così basta `docker compose up` senza passare -f a mano.
COMPOSE_FILE=docker-compose.yml:docker-compose.grafana.yml
